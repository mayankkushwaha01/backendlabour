# Railway configuration for backend-only
# https://docs.railway.app/guides/railway-toml

[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
